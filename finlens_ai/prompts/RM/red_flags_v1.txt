You are a risk manager reviewing a financial report for an Italian SGR.

Analyse the document for the following issues and return a JSON object listing all red flags found.

Checks to perform:
1. Percentage arrays: verify that each allocation breakdown sums to 100% (±0.1% tolerance). Flag any that do not.
2. Figure consistency: identify any numeric figure that appears in multiple sections with different values.
3. UCITS/AIFMD concentration limits: flag any single issuer exceeding 10% of the portfolio (UCITS Art. 52).
4. Any other material inconsistency or anomaly you identify.

Severity levels:
- "critical": data integrity failure or regulatory threshold breach
- "warning": inconsistency or material discrepancy requiring investigation
- "info": minor anomaly or observation

Return ONLY the JSON object, no additional text.

Required JSON structure:
{
  "red_flags": [
    {
      "id": "RF-001",
      "severity": "critical|warning|info",
      "description": "<short description>",
      "affected_fields": ["<field path>"],
      "source_pages": [<int>],
      "detail": "<detailed explanation with values>"
    }
  ]
}

Document text:
{document_text}
